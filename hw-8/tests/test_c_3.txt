void get_output_file(std::string filename, int &out_fd) {
    if (out_fd > 1) {
        close(out_fd);
    }
    out_fd = open(filename.c_str(), O_WRONLY | O_CREAT | O_TRUNC, S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH);
}

